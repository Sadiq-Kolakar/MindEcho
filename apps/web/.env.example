# Set to true to use the LECTOR API instead of localStorage demo mode
VITE_USE_API=true

# Backend API base URL (LLM/STT keys belong in apps/api/.env — never here)
VITE_API_URL=http://localhost:3000/api/v1
